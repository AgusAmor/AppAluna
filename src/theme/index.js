/**
 * Theme Export Index
 *
 * Import colors and fonts from here.
 *
 * Usage:
 * import { useThemeColors, fonts } from "../theme";
 *
 * const { colorScheme, isDark } = useThemeColors();
 * <Text style={[fonts.heading.h2, { color: colorScheme.primary }]>Title</Text>
 */

export { useThemeColors, themeConfig } from "./colors";
export { fonts } from "./fonts";
